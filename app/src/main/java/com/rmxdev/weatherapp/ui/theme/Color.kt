package com.rmxdev.weatherapp.ui.theme

import androidx.compose.ui.graphics.Color

// Background
val BackgroundBlue = Color(0xFF1472DB)

// Cards
val CardBorder = Color(0xFFE0E3E6)
val CardBackground = Color(0xFFF3F8FF)

// Skeleton
val SkeletonOverlay = Color(0x1A071C36)

// Text
val TextPrimary = Color(0xFF47515C)
val TextWhite = Color(0xFFFFFFFF)

// Sun
val SunYellow = Color(0xFFFDD83B)
val SunOrange = Color(0xFFF9AE00)

// Circular stats arcs
val ArcPressure = Color(0xFFF9AE00)
val ArcClouds = Color(0xFFFF5656)
val ArcHumidity = Color(0xFF40FFFC)

// Wind arc
val ArcWindLeft = Color(0xFFFFC940)

// Compass
val CompassNeedle = Color(0xFFFF5C32)

// Misc
val CloudWhite = Color(0xFFFEFFFF)